/*
import { get, post, del } from './client';

export const domainsApi = {
  list:   ()   => get('/domains/'),
  get:    (id) => get(`/domains/${id}/`),
  create: (d)  => post('/domains/', d),
  verify: (id) => post(`/domains/${id}/verify/`),
  delete: (id) => del(`/domains/${id}/`),
};
*/