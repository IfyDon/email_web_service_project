/*
import { get, post, del } from './client';

export const suppressionsApi = {
  list:   (params) => get('/suppressions/',       params),
  add:    (data)   => post('/suppressions/',       data),
  delete: (id)     => del(`/suppressions/${id}/`),
  check:  (email)  => post('/suppressions/check/', { email }),
};
*/