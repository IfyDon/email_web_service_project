/*
import { clsx } from 'clsx';
export default function Card({ children, className, noPad }) {
  return <div className={clsx('bg-white rounded-xl border border-gray-200 shadow-sm', !noPad && 'p-6', className)}>{children}</div>;
}
*/